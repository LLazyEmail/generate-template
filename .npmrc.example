# Copy to ~/.npmrc or a project .npmrc when installing this package.
# GitHub Packages requires a PAT (read:packages) even for public packages.

@llazyemail:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
